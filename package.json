{
	"name": "fb-webhook",
	"version": "1.0.0",
	"description": "facebook messenger custom chatbot webhook",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "NODE_ENV=production node server.js",
		"dev": "NODE_ENV=development nodemon server.js"
	},
	"license": "MIT",
	"dependencies": {
		"dotenv": "^16.0.3",
		"express": "^4.18.2",
		"morgan": "^1.10.0",
		"request": "^2.88.2"
	},
	"devDependencies": {
		"nodemon": "^2.0.20"
	}
}